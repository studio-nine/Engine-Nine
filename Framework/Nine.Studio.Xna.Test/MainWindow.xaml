<Window x:Class="Nine.Studio.Xna.Test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ad="clr-namespace:AvalonDock;assembly=AvalonDock"
        
        xmlns:wf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
            <!--
        <g:GameHost x:Name="GameHost">
            <g:GameHost.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Click Me!" />
                </ContextMenu>
            </g:GameHost.ContextMenu>
        </g:GameHost>
            -->
        <!--
        <ad:DockingManager x:Name="DockingManager" Grid.Row="2" DocumentsSource="{Binding DocumentContents}" ActiveDocument="{Binding ActiveDocument}">
            <ad:ResizingPanel Orientation="Vertical">
                <ad:DocumentPane>
                    <ad:DocumentContent>
                    </ad:DocumentContent>
                </ad:DocumentPane>
                <ad:DockablePane x:Name="DockHost">
                </ad:DockablePane>
            </ad:ResizingPanel>
        </ad:DockingManager>
        -->
    </Grid>
</Window>
