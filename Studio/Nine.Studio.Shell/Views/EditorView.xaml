<DockPanel
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:n="http://schemas.microsoft.com/nine/2011/xaml"
    xmlns:my="clr-namespace:Nine.Studio.Shell"
    xmlns:sys="clr-namespace:System.Collections;assembly=mscorlib"
    mc:Ignorable="d" d:DesignWidth="1280" d:DesignHeight="720"
    x:Name="LayoutRoot" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    
    <StackPanel Name="InspectorFrame" Background="#FF1C1C1C" Width="60" DockPanel.Dock="Right">
        <WrapPanel Name="QuickTools">
            <Button Style="{StaticResource Button-LargeIcon}" n:WindowChrome.IsHitTestVisibleInChrome="True">&#xE071;</Button>
        </WrapPanel>
    </StackPanel>

    <Grid Name="MainFrame" Background="#FF2D2D30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="120"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="160"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>

        <StackPanel Name="WindowsButtons" Grid.Column="1" Orientation="Horizontal" Height="32" Width="96">
            <Button Style="{StaticResource Button-MediumIcon}" n:WindowChrome.IsHitTestVisibleInChrome="True" FontSize="9">&#x2581;</Button>
            <Button Style="{StaticResource Button-MediumIcon}" n:WindowChrome.IsHitTestVisibleInChrome="True">&#x2750;</Button>
            <Button Style="{StaticResource Button-MediumIcon}" n:WindowChrome.IsHitTestVisibleInChrome="True">&#x2A09;</Button>
        </StackPanel>
        
        <StackPanel Name="TitleBar" Grid.ColumnSpan="2" Orientation="Horizontal">
            <Button Name="Library" Margin="20,0" Padding="20,0" Style="{StaticResource Button-Title}" Content="{Binding Title}" />
            <n:WatermarkTextBox Name="Search" Watermark="Search..." />
        </StackPanel>
        
        <Grid Name="StatusBar" Grid.Row="3" Background="{StaticResource Brush-Accent}" Grid.ColumnSpan="2" />
    </Grid>
    
</DockPanel>