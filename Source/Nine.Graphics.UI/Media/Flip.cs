namespace Nine.Graphics.UI.Media
{
    // Same as SpriteEffects. 
    // Should we use it instead?
    public enum Flip
    {
        None,
        Horizontally,
        Vertically,
        Both
    }
}
