<UserControl x:Class="Nine.Studio.Shell.NewProjectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:n="clr-namespace:Nine.Studio.Shell" mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" Width="340" x:Name="userControl">
    <StackPanel Margin="0,20">
        <TextBox n:Header.Header="{x:Static n:Strings.Name}" Text="{Binding Path=ProjectName, ElementName=userControl}" />
        <TextBox n:Header.Header="{x:Static n:Strings.Location}" Text="{Binding Path=ProjectDirectory, ElementName=userControl}" />
    </StackPanel>
</UserControl>
