<Window x:Class="Nine.Studio.Shell.WaitWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Nine.Studio.Shell"
        Title="{x:Static local:Strings.PleaseWait}" Height="160" Width="500" ShowInTaskbar="False" Cursor="Wait"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" WindowStyle="SingleBorderWindow" Background="#FF656565">

    <Grid Margin="12" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TextBox" Foreground="White" HorizontalAlignment="Stretch" VerticalAlignment="Center" Margin="0,32,0,0" />
        <ProgressBar x:Name="ProgressBar" Height="12" HorizontalAlignment="Stretch" VerticalAlignment="Center" Grid.Row="1" IsIndeterminate="True" />
    </Grid>

</Window>
